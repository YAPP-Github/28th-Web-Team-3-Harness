# API Versioning

이 문서는 운영 환경에서 사용하는 백엔드 API와 프론트엔드의 버전 호환 범위를 관리한다.
프론트엔드와 백엔드 개발진은 배포 전에 아래 표에서 배포하려는 버전의 상호 호환 여부를 확인한다.

## 버전 호환성

| 백엔드 | 프론트엔드 |
| --- | --- |
| `1.0.x` | `1.0.x` |

표의 각 행은 서로 호환되는 백엔드와 프론트엔드 버전 범위를 뜻한다.
한쪽 버전이 해당 행의 범위를 벗어나면, 호환성이 확인되어 표가 갱신되기 전까지 호환되지 않는 것으로 간주한다.

## 버전 범위 표기 규칙

- `1.0.x`: `1.0.0` 이상 `1.1.0` 미만의 모든 패치 버전
- `^1.0.0`: `1.0.0`을 포함하여 그 이후의 모든 버전
- `>=1.0.0 <2.0.0`: `1.0.0` 이상 `2.0.0` 미만의 모든 버전
- `1.0.x || 2.0.x`: 두 범위 중 하나에 해당하는 버전

`^`는 이 문서에서 위와 같이 정의한 팀 표기 규칙이며, 패키지 매니저별 버전 범위 해석과 다를 수 있다.

## 관리 규칙

- 이 문서는 운영 환경에 배포된 버전을 기준으로 개발진이 직접 갱신한다.
- 호환 범위가 변경되면 기존 행을 임의로 덮어쓰지 않고 새 호환 범위를 확인할 수 있도록 표를 갱신한다.
- 프론트엔드 또는 백엔드의 메이저·마이너 버전을 변경하기 전에 상대 버전과의 호환성을 검증한다.
